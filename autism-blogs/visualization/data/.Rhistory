fulldata$weekBirthShift <- NULL;
fulldata$week <- as.Date(fulldata$week);
fulldata$year <- format(fulldata$week, "%Y");
fulldata$week <- NULL;
agYearly <- aggregate(x = fulldata, by = list(fulldata$blogid, fulldata$year), FUN = mean);
agYearly$year <- agYearly$Group.2;
agYearly$Group.1 <- NULL;
agYearly$Group.2 <- NULL;
write.csv(agYearly, file = "agYearly.csv", row.names=FALSE);
file.remove("yearlydata.js");
file.create("yearlydata.js");
tojs <- "var yearlydata = [";
for (i in c(1:length(agYearly[,1]))) {
tojs <- paste(tojs, "[", sep = " ");
for (j in c(1: length(agYearly[i,]))) {
tojs <- paste(tojs, agYearly[i, j], sep = " ");
if (j < length(agYearly[i,]) - 1) {
tojs <- paste(tojs, ",", sep = "");
}
}
tojs <- paste(tojs, "]", sep = "");
if (i < length(agYearly[,1]) - 1) {
tojs <- paste(tojs, ",", sep = "");
}
}
write(x = tojs, file = "yearlydata.js", append = TRUE);
# Re-processing the data and creating a Javascript file for the purposes
# of displaying data year-by-year
fulldata <- read.csv("ag.csv");
fulldata$tenureOffset <- NULL;
fulldata$birthOffset <- NULL;
fulldata$weekTenureShift <- NULL;
fulldata$weekBirthShift <- NULL;
fulldata$week <- as.Date(fulldata$week);
fulldata$year <- format(fulldata$week, "%Y");
fulldata$week <- NULL;
agYearly <- aggregate(x = fulldata, by = list(fulldata$blogid, fulldata$year), FUN = mean);
agYearly$year <- agYearly$Group.2;
agYearly$Group.1 <- NULL;
agYearly$Group.2 <- NULL;
write.csv(agYearly, file = "agYearly.csv", row.names=FALSE);
file.remove("yearlydata.js");
file.create("yearlydata.js");
tojs <- "var yearlydata = [";
for (i in c(1:length(agYearly[,1]))) {
tojs <- paste(tojs, "[", sep = " ");
for (j in c(1: length(agYearly[i,]))) {
tojs <- paste(tojs, agYearly[i, j], sep = " ");
if (j < length(agYearly[i,]) - 1) {
tojs <- paste(tojs, ",", sep = "");
}
}
tojs <- paste(tojs, "]", sep = "");
if (i < length(agYearly[,1]) - 1) {
tojs <- paste(tojs, ",", sep = "");
}
}
write(x = tojs, file = "yearlydata.js", append = TRUE);
# Re-processing the data and creating a Javascript file for the purposes
# of displaying data year-by-year
fulldata <- read.csv("ag.csv");
fulldata$tenureOffset <- NULL;
fulldata$birthOffset <- NULL;
fulldata$weekTenureShift <- NULL;
fulldata$weekBirthShift <- NULL;
fulldata$week <- as.Date(fulldata$week);
fulldata$year <- format(fulldata$week, "%Y");
fulldata$week <- NULL;
agYearly <- aggregate(x = fulldata, by = list(fulldata$blogid, fulldata$year), FUN = mean);
agYearly$year <- agYearly$Group.2;
agYearly$Group.1 <- NULL;
agYearly$Group.2 <- NULL;
write.csv(agYearly, file = "agYearly.csv", row.names=FALSE);
file.remove("yearlydata.js");
file.create("yearlydata.js");
tojs <- "var yearlydata = [";
for (i in c(1:length(agYearly[,1]))) {
tojs <- "";
tojs <- paste(tojs, "[", sep = " ");
for (j in c(1: length(agYearly[i,]))) {
tojs <- paste(tojs, agYearly[i, j], sep = " ");
if (j < length(agYearly[i,]) - 1) {
tojs <- paste(tojs, ",", sep = "");
}
}
tojs <- paste(tojs, "]", sep = "");
if (i < length(agYearly[,1]) - 1) {
tojs <- paste(tojs, ",", sep = "");
}
write(x = tojs, file = "yearlydata.js", append = TRUE);
}
write(x = tojs, file = "yearlydata.js", append = TRUE);
# Re-processing the data and creating a Javascript file for the purposes
# of displaying data year-by-year
fulldata <- read.csv("ag.csv");
fulldata$tenureOffset <- NULL;
fulldata$birthOffset <- NULL;
fulldata$weekTenureShift <- NULL;
fulldata$weekBirthShift <- NULL;
fulldata$week <- as.Date(fulldata$week);
fulldata$year <- format(fulldata$week, "%Y");
fulldata$week <- NULL;
agYearly <- aggregate(x = fulldata, by = list(fulldata$blogid, fulldata$year), FUN = mean);
agYearly$year <- agYearly$Group.2;
agYearly$Group.1 <- NULL;
agYearly$Group.2 <- NULL;
write.csv(agYearly, file = "agYearly.csv", row.names=FALSE);
file.remove("yearlydata.js");
file.create("yearlydata.js");
tojs <- "var yearlydata = [";
write(x = tojs, file = "yearlydata.js", append = TRUE);
for (i in c(1:length(agYearly[,1]))) {
tojs <- "";
tojs <- paste(tojs, "[", sep = " ");
for (j in c(1: length(agYearly[i,]))) {
tojs <- paste(tojs, agYearly[i, j], sep = " ");
if (j < length(agYearly[i,]) - 1) {
tojs <- paste(tojs, ",", sep = "");
}
}
tojs <- paste(tojs, "]", sep = "");
if (i < length(agYearly[,1]) - 1) {
tojs <- paste(tojs, ",", sep = "");
}
write(x = tojs, file = "yearlydata.js", append = TRUE);
}
write(x = tojs, file = "yearlydata.js", append = TRUE);
# Re-processing the data and creating a Javascript file for the purposes
# of displaying data year-by-year
fulldata <- read.csv("ag.csv");
fulldata$tenureOffset <- NULL;
fulldata$birthOffset <- NULL;
fulldata$weekTenureShift <- NULL;
fulldata$weekBirthShift <- NULL;
fulldata$week <- as.Date(fulldata$week);
fulldata$year <- format(fulldata$week, "%Y");
fulldata$week <- NULL;
agYearly <- aggregate(x = fulldata, by = list(fulldata$blogid, fulldata$year), FUN = mean);
agYearly$year <- agYearly$Group.2;
agYearly$Group.1 <- NULL;
agYearly$Group.2 <- NULL;
write.csv(agYearly, file = "agYearly.csv", row.names=FALSE);
file.remove("yearlydata.js");
file.create("yearlydata.js");
tojs <- "var yearlydata = [";
write(x = tojs, file = "yearlydata.js", append = TRUE);
for (i in c(1:length(agYearly[,1]))) {
tojs <- "";
tojs <- paste(tojs, "[", sep = " ");
for (j in c(1: length(agYearly[i,]))) {
tojs <- paste(tojs, agYearly[i, j], sep = " ");
if (j < length(agYearly[i,]) - 1) {
tojs <- paste(tojs, ",", sep = "");
}
}
tojs <- paste(tojs, "]", sep = "");
if (i < length(agYearly[,1]) - 1) {
tojs <- paste(tojs, ",", sep = "");
}
write(x = tojs, file = "yearlydata.js", append = TRUE);
}
write(x = tojs, file = "yearlydata.js", append = TRUE);
file.remove("yearlydata.js");
file.create("yearlydata.js");
tojs <- "var yearlydata = [";
write(x = tojs, file = "yearlydata.js", append = TRUE);
for (i in c(1:length(agYearly[,1]))) {
tojs <- "";
tojs <- paste(tojs, "[", sep = " ");
for (j in c(1: length(agYearly[i,]))) {
tojs <- paste(tojs, agYearly[i, j], sep = " ");
if (j < length(agYearly[i,])) {
tojs <- paste(tojs, ",", sep = "");
}
}
tojs <- paste(tojs, "]", sep = "");
if (i < length(agYearly[,1])) {
tojs <- paste(tojs, ",", sep = "");
}
write(x = tojs, file = "yearlydata.js", append = TRUE);
}
write(x = tojs, file = "yearlydata.js", append = TRUE);
file.remove("yearlydata.js");
file.create("yearlydata.js");
tojs <- "var yearlydata = [";
write(x = tojs, file = "yearlydata.js", append = TRUE);
for (i in c(1:length(agYearly[,1]))) {
tojs <- "";
tojs <- paste(tojs, "[", sep = " ");
for (j in c(1: length(agYearly[i,]))) {
tojs <- paste(tojs, agYearly[i, j], sep = " ");
if (j < length(agYearly[i,])) {
tojs <- paste(tojs, ",", sep = "");
}
}
tojs <- paste(tojs, "]", sep = "");
if (i < length(agYearly[,1]) - 1) {
file.remove("yearlydata.js");
file.create("yearlydata.js");
tojs <- "var yearlydata = [";
write(x = tojs, file = "yearlydata.js", append = TRUE);
for (i in c(1:length(agYearly[,1]))) {
tojs <- "";
tojs <- paste(tojs, "[", sep = " ");
for (j in c(1: length(agYearly[i,]))) {
tojs <- paste(tojs, agYearly[i, j], sep = " ");
if (j < length(agYearly[i,])) {
tojs <- paste(tojs, ",", sep = "");
}
}
tojs <- paste(tojs, "]", sep = "");
if (i < length(agYearly[,1]) - 1) {
tojs <- paste(tojs, ",", sep = "");
}
write(x = tojs, file = "yearlydata.js", append = TRUE);
}
write(x = tojs, file = "yearlydata.js", append = TRUE);
file.remove("yearlydata.js");
file.create("yearlydata.js");
tojs <- "var yearlydata = [";
write(x = tojs, file = "yearlydata.js", append = TRUE);
for (i in c(1:length(agYearly[,1]))) {
tojs <- "";
tojs <- paste(tojs, "[", sep = " ");
for (j in c(1: length(agYearly[i,]))) {
tojs <- paste(tojs, agYearly[i, j], sep = " ");
if (j < length(agYearly[i,])) {
tojs <- paste(tojs, ",", sep = "");
}
}
tojs <- paste(tojs, "]", sep = "");
if (i < length(agYearly[,1]) - 1) {
tojs <- paste(tojs, ",", sep = "");
}
write(x = tojs, file = "yearlydata.js", append = TRUE);
}
# Re-processing the data and creating a Javascript file for the purposes
# of displaying data year-by-year
fulldata <- read.csv("ag.csv");
fulldata$tenureOffset <- NULL;
fulldata$birthOffset <- NULL;
fulldata$weekTenureShift <- NULL;
fulldata$weekBirthShift <- NULL;
fulldata$week <- as.Date(fulldata$week);
fulldata$year <- format(fulldata$week, "%Y");
fulldata$week <- NULL;
agYearly <- aggregate(x = fulldata, by = list(fulldata$blogid, fulldata$year), FUN = mean);
agYearly$year <- agYearly$Group.2;
agYearly$Group.1 <- NULL;
agYearly$Group.2 <- NULL;
write.csv(agYearly, file = "agYearly.csv", row.names=FALSE);
file.remove("yearlydata.js");
file.create("yearlydata.js");
tojs <- "var yearlydata = [";
write(x = tojs, file = "yearlydata.js", append = TRUE);
for (i in c(1:length(agYearly[,1]))) {
tojs <- "";
tojs <- paste(tojs, "[", sep = " ");
for (j in c(1: length(agYearly[i,]))) {
tojs <- paste(tojs, agYearly[i, j], sep = " ");
if (j < length(agYearly[i,])) {
tojs <- paste(tojs, ",", sep = "");
}
}
tojs <- paste(tojs, "]", sep = "");
if (i < length(agYearly[,1]) - 1) {
tojs <- paste(tojs, ",", sep = "");
}
write(x = tojs, file = "yearlydata.js", append = TRUE);
}
write(x = tojs, file = "yearlydata.js", append = TRUE);
fulldata <- read.csv("ag.csv");
fulldata$tenureOffset <- NULL;
fulldata$birthOffset <- NULL;
fulldata$weekTenureShift <- NULL;
fulldata$weekBirthShift <- NULL;
fulldata$week <- as.Date(fulldata$week);
fulldata$year <- format(fulldata$week, "%Y");
fulldata$week <- NULL;
agYearly <- aggregate(x = fulldata, by = list(fulldata$blogid, fulldata$year), FUN = mean);
agYearly$year <- agYearly$Group.2;
agYearly$Group.1 <- NULL;
agYearly$Group.2 <- NULL;
write.csv(agYearly, file = "agYearly.csv", row.names=FALSE);
file.remove("yearlydata.js");
file.create("yearlydata.js");
fulldata <- read.csv("ag.csv");
fulldata$tenureOffset <- NULL;
fulldata$birthOffset <- NULL;
fulldata$weekTenureShift <- NULL;
fulldata$weekBirthShift <- NULL;
fulldata$week <- as.Date(fulldata$week);
fulldata$year <- format(fulldata$week, "%Y");
fulldata$week <- NULL;
agYearly <- aggregate(x = fulldata, by = list(fulldata$blogid, fulldata$year), FUN = mean);
agYearly$year <- agYearly$Group.2;
agYearly$Group.1 <- NULL;
agYearly$Group.2 <- NULL;
write.csv(agYearly, file = "agYearly.csv", row.names=FALSE);
file.remove("yearlydata.js");
file.create("yearlydata.js");
# Re-processing the data and creating a Javascript file for the purposes
# of displaying data year-by-year
fulldata <- read.csv("ag.csv");
fulldata$tenureOffset <- NULL;
fulldata$birthOffset <- NULL;
fulldata$weekTenureShift <- NULL;
fulldata$weekBirthShift <- NULL;
fulldata$week <- as.Date(fulldata$week);
fulldata$year <- format(fulldata$week, "%Y");
fulldata$week <- NULL;
agYearly <- aggregate(x = fulldata, by = list(fulldata$blogid, fulldata$year), FUN = mean);
agYearly$year <- agYearly$Group.2;
agYearly$Group.1 <- NULL;
agYearly$Group.2 <- NULL;
write.csv(agYearly, file = "agYearly.csv", row.names=FALSE);
file.remove("yearlydata.js");
file.create("yearlydata.js");
tojs <- "var yearlydata = [";
write(x = tojs, file = "yearlydata.js", append = TRUE);
for (i in c(1:length(agYearly[,1]))) {
tojs <- "";
tojs <- paste(tojs, "[", sep = " ");
for (j in c(1: length(agYearly[i,]))) {
tojs <- paste(tojs, agYearly[i, j], sep = " ");
if (j < length(agYearly[i,])) {
tojs <- paste(tojs, ",", sep = "");
}
}
tojs <- paste(tojs, "]", sep = "");
if (i < length(agYearly[,1]) - 1) {
tojs <- paste(tojs, ",", sep = "");
}
write(x = tojs, file = "yearlydata.js", append = TRUE);
}
write(x = tojs, file = "yearlydata.js", append = TRUE);
# Re-processing the data and creating a Javascript file for the purposes
# of displaying data year-by-year
fulldata <- read.csv("ag.csv");
fulldata$tenureOffset <- NULL;
fulldata$birthOffset <- NULL;
fulldata$weekTenureShift <- NULL;
fulldata$weekBirthShift <- NULL;
fulldata$week <- as.Date(fulldata$week);
fulldata$year <- format(fulldata$week, "%Y");
fulldata$week <- NULL;
agYearly <- aggregate(x = fulldata, by = list(fulldata$blogid, fulldata$year), FUN = mean);
agYearly$year <- agYearly$Group.2;
agYearly$Group.1 <- NULL;
agYearly$Group.2 <- NULL;
write.csv(agYearly, file = "agYearly.csv", row.names=FALSE);
file.remove("yearlydata.js");
file.create("yearlydata.js");
tojs <- "var yearlydata = [";
write(x = tojs, file = "yearlydata.js", append = TRUE);
for (i in c(1:length(agYearly))) {
tojs <- "";
tojs <- paste(tojs, "[", sep = " ");
for (j in c(1: length(agYearly[i,]))) {
tojs <- paste(tojs, agYearly[i, j], sep = " ");
if (j < length(agYearly[i,])) {
tojs <- paste(tojs, ",", sep = "");
}
}
tojs <- paste(tojs, "]", sep = "");
if (i < length(agYearly[,1]) - 1) {
tojs <- paste(tojs, ",", sep = "");
}
write(x = tojs, file = "yearlydata.js", append = TRUE);
}
write(x = tojs, file = "yearlydata.js", append = TRUE);
# Re-processing the data and creating a Javascript file for the purposes
# of displaying data year-by-year
fulldata <- read.csv("ag.csv");
fulldata$tenureOffset <- NULL;
fulldata$birthOffset <- NULL;
fulldata$weekTenureShift <- NULL;
fulldata$weekBirthShift <- NULL;
fulldata$week <- as.Date(fulldata$week);
fulldata$year <- format(fulldata$week, "%Y");
fulldata$week <- NULL;
agYearly <- aggregate(x = fulldata, by = list(fulldata$blogid, fulldata$year), FUN = mean);
agYearly$year <- agYearly$Group.2;
agYearly$Group.1 <- NULL;
agYearly$Group.2 <- NULL;
write.csv(agYearly, file = "agYearly.csv", row.names=FALSE);
file.remove("yearlydata.js");
file.create("yearlydata.js");
tojs <- "var yearlydata = [";
write(x = tojs, file = "yearlydata.js", append = TRUE);
for (i in c(1:length(agYearly[1,]))) {
tojs <- "";
tojs <- paste(tojs, "[", sep = " ");
for (j in c(1: length(agYearly[i,]))) {
tojs <- paste(tojs, agYearly[i, j], sep = " ");
if (j < length(agYearly[i,])) {
tojs <- paste(tojs, ",", sep = "");
}
}
tojs <- paste(tojs, "]", sep = "");
if (i < length(agYearly[,1]) - 1) {
tojs <- paste(tojs, ",", sep = "");
}
write(x = tojs, file = "yearlydata.js", append = TRUE);
}
write(x = tojs, file = "yearlydata.js", append = TRUE);
# Re-processing the data and creating a Javascript file for the purposes
# of displaying data year-by-year
fulldata <- read.csv("ag.csv");
fulldata$tenureOffset <- NULL;
fulldata$birthOffset <- NULL;
fulldata$weekTenureShift <- NULL;
fulldata$weekBirthShift <- NULL;
fulldata$week <- as.Date(fulldata$week);
fulldata$year <- format(fulldata$week, "%Y");
fulldata$week <- NULL;
agYearly <- aggregate(x = fulldata, by = list(fulldata$blogid, fulldata$year), FUN = mean);
agYearly$year <- agYearly$Group.2;
agYearly$Group.1 <- NULL;
agYearly$Group.2 <- NULL;
write.csv(agYearly, file = "agYearly.csv", row.names=FALSE);
file.remove("yearlydata.js");
file.create("yearlydata.js");
tojs <- "var yearlydata = [";
write(x = tojs, file = "yearlydata.js", append = TRUE);
for (i in c(1:length(agYearly[,1]))) {
tojs <- "";
tojs <- paste(tojs, "[", sep = " ");
for (j in c(1: length(agYearly[i,]))) {
tojs <- paste(tojs, agYearly[i, j], sep = " ");
if (j < length(agYearly[i,])) {
tojs <- paste(tojs, ",", sep = "");
}
}
tojs <- paste(tojs, "]", sep = "");
if (i < length(agYearly[,1]) - 1) {
tojs <- paste(tojs, ",", sep = "");
}
write(x = tojs, file = "yearlydata.js", append = TRUE);
}
write(x = tojs, file = "yearlydata.js", append = TRUE);
# Re-processing the data and creating a Javascript file for the purposes
# of displaying data year-by-year
fulldata <- read.csv("ag.csv");
fulldata$tenureOffset <- NULL;
fulldata$birthOffset <- NULL;
fulldata$weekTenureShift <- NULL;
fulldata$weekBirthShift <- NULL;
fulldata$week <- as.Date(fulldata$week);
fulldata$year <- format(fulldata$week, "%Y");
fulldata$week <- NULL;
agYearly <- aggregate(x = fulldata, by = list(fulldata$blogid, fulldata$year), FUN = mean);
agYearly$year <- agYearly$Group.2;
agYearly$Group.1 <- NULL;
agYearly$Group.2 <- NULL;
write.csv(agYearly, file = "agYearly.csv", row.names=FALSE);
file.remove("yearlydata.js");
file.create("yearlydata.js");
tojs <- "var yearlydata = [";
write(x = tojs, file = "yearlydata.js", append = TRUE);
for (i in c(1:length(agYearly[,1]))) {
tojs <- "";
tojs <- paste(tojs, "[", sep = " ");
for (j in c(1: length(agYearly[i,]))) {
tojs <- paste(tojs, agYearly[i, j], sep = " ");
if (j < length(agYearly[i,])) {
tojs <- paste(tojs, ",", sep = "");
}
}
tojs <- paste(tojs, "]", sep = "");
if (i < length(agYearly[,1])) {
tojs <- paste(tojs, ",", sep = "");
}
write(x = tojs, file = "yearlydata.js", append = TRUE);
}
write(x = tojs, file = "yearlydata.js", append = TRUE);
# Re-processing the data and creating a Javascript file for the purposes
# of displaying data year-by-year
fulldata <- read.csv("ag.csv");
fulldata$tenureOffset <- NULL;
fulldata$birthOffset <- NULL;
fulldata$weekTenureShift <- NULL;
fulldata$weekBirthShift <- NULL;
fulldata$week <- as.Date(fulldata$week);
fulldata$year <- format(fulldata$week, "%Y");
fulldata$week <- NULL;
agYearly <- aggregate(x = fulldata, by = list(fulldata$blogid, fulldata$year), FUN = mean);
agYearly$year <- agYearly$Group.2;
agYearly$Group.1 <- NULL;
agYearly$Group.2 <- NULL;
write.csv(agYearly, file = "agYearly.csv", row.names=FALSE);
file.remove("yearlydata.js");
file.create("yearlydata.js");
tojs <- "var yearlydata = [";
write(x = tojs, file = "yearlydata.js", append = TRUE);
for (i in c(1:length(agYearly[,1]))) {
tojs <- "";
tojs <- paste(tojs, "[", sep = " ");
for (j in c(1: length(agYearly[i,]))) {
tojs <- paste(tojs, agYearly[i, j], sep = " ");
if (j < length(agYearly[i,])) {
tojs <- paste(tojs, ",", sep = "");
}
}
tojs <- paste(tojs, "]", sep = "");
if (i < length(agYearly[,1])) {
tojs <- paste(tojs, ",", sep = "");
}
write(x = tojs, file = "yearlydata.js", append = TRUE);
}
tojs <- "]";
write(x = tojs, file = "yearlydata.js", append = TRUE);
